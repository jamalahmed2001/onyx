node_modules/
dist/
.env
*.DS_Store
._*
.onyx-locks/
.onyx-checkpoints/
.onyx-cursor-sessions/
.onyx-backups/
*.jsonl

# Dashboard build artifacts
dashboard/.next/
dashboard/node_modules/
dashboard/tsconfig.tsbuildinfo

# Dashboard: hand-tracking assets (large binaries — run `npm run setup:hand`)
dashboard/public/mediapipe/

# Dashboard: local HTTPS certificates (run `npm run setup:hand` to generate)
dashboard/certificates/

# Vault viewer cache / generated
vault-viewer.html

# Local demo/test vault artifacts (keep vault content if you want, but never commit Obsidian settings)
vault/.obsidian/
vault/.trash/

# Skills node_modules
skills/**/node_modules/

# Dashboard: local dev secrets (never commit)
dashboard/.env.local
dashboard/mailcow-accounts.json

# Local Claude Code harness state (session locks, per-machine permissions)
.claude/

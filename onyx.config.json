{
  "_comment": "ONYX config. Secrets go in .env (never commit .env). Set vault_root here OR via ONYX_VAULT_ROOT in .env.",
  "vault_root": "<home>/Obsidian/OnyxVault",
  "agent_driver": "claude-code",
  "_agent_driver_options": "claude-code | cursor",
  "llm": {
    "_comment": "Any model on https://openrouter.ai/models works. Set OPENROUTER_API_KEY in .env — no key needed here.",
    "model": "anthropic/claude-sonnet-4-6"
  },
  "model_tiers": {
    "_comment": "planning = phase decomposition + task atomisation (always uses the best model). light/standard/heavy = execution tiers, auto-classified per task.",
    "planning": "anthropic/claude-opus-4-6",
    "light": "anthropic/claude-haiku-4-5-20251001",
    "standard": "anthropic/claude-sonnet-4-6",
    "heavy": "anthropic/claude-opus-4-6"
  },
  "max_iterations": 20,
  "stale_lock_threshold_ms": 300000,
  "projects_glob": "{02 - <workplace>/**,03 - Ventures/**,05 - Finance/**,10 - OpenClaw/**}",
  "repos_root": "<home>/clawd/openclaw/projects",
  "notify": {
    "stdout": true,
    "_openclaw_comment": "Set target to your E.164 number. Requires openclaw CLI installed + logged in. Alternatively set OPENCLAW_NOTIFY_TARGET env var.",
    "_openclaw": {
      "target": "+447..."
    }
  },
  "_linear_comment": "Uncomment and fill in to enable Linear integration (onyx import + phase uplink).",
  "_linear": {
    "enabled": true,
    "api_key": "lin_api_...",
    "team_id": "YOUR_TEAM_ID"
  },
  "location": {
    "lat": 51.5074,
    "lng": -0.1278
  },
  "linear": {
    "_comment": "Credentials come from LINEAR_API_KEY and LINEAR_TEAM_ID env vars (set in .env, never commit).",
    "enabled": true
  }
}